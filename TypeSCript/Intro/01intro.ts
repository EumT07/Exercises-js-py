let user = {
    name: "Eublan",
    age: 30
}

console.log("Eublan");
console.log(user.age);
// console.log(user.email); 
//This erro is because we don't create email property yet, so this property doesn't exit for the moments
// 